# portexercise

Steps to run:

git clone https://github.com/ArchanaY/portexercise.git

The below command would start both client and server
src\portexercise\deploy>docker-compose up --build

Run the tests in tests\testing.ps1

